description: >
  Compile a tex file into pdf. Outputs result to artifacts.

usage:
  version: 2.1
  orbs:
    latex-orb: woltsu/latex-orb@x.y.z
  workflows:
    compile:
      jobs:
        - latex-orb/compile:
            file: /path/to/file.tex
