description: >
  Convert .tex file into pdf

steps:
  - docker/install-docker
  - docker -v
  - docker run schickling/latex pdflatex
  #- run:
  #    name: Test
  #    shell: /bin/bash
  #    command: |
  #      docker run schickling/latex pdflatex
  - store_artifacts:
      path: /source
