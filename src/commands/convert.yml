description: >
  Convert .tex file into pdf

executor: latex

steps:
  - run:
      name: Test
      shell: /bin/bash
      command: |
        mkdir /tmp/latex
        echo "TEST" > /tmp/latex/test.txt
  - store_artifacts:
      path: /tmp/latex
